import styled from 'styled-components';

const AppStyle = styled.div`
    width: 100vw;
    height: 100vh;

    background: url("/images/default_background_win10.jpg") no-repeat;
    background-size: cover;
`;

export default AppStyle;